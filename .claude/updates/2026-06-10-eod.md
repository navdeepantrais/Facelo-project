# EOD Report — 10 June 2026

## Login Page

- Applied two-column auth layout (marketing panel + form) matching the register page
- Replaced inline Google OAuth handler with shared OAuthButtons component
- Replaced plain password input with PasswordInput (show/hide toggle)
- Fixed pre-existing ESLint crash (nextVitals not iterable) using FlatCompat

## Auth Flow — Signup

- Signup now creates a matching `public.users` profile immediately after Supabase auth user is created
- Reused existing `upsertUser` helper — handles duplicates safely with `onConflictDoUpdate`

## Auth Flow — Login

- Login now validates that a `public.users` profile exists before granting access; signs out and returns error if missing
- Added account status check — blocked, suspended, and deleted accounts are signed out with a specific error message
- Creator routing changed from role field check to a live `creators` table lookup

## Database

- Added `account_status` enum with values: active, blocked, suspended, deleted
- Added `status` column to `users` table (default: active, indexed)
- Generated and applied migration — all existing rows auto-set to active

## Become a Creator

- Replaced single-click button with a proper form
- Form collects: bio, promo code, Instagram / YouTube / TikTok URLs
- All fields optional; promo code lowercased and validated server-side
- Promo code uniqueness conflict returns a field-level error instead of crashing
- Race condition on concurrent submissions handled with targeted conflict ignore on userId only
